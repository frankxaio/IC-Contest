#!/bin/bash

# Get the current working directory (where the script is sourced from)
ROOT_PATH="$(pwd)"

# Export ROOT_PATH as an environment variable
export ROOT_PATH

# Print the value for verification
echo "set Project ROOT_PATH: $ROOT_PATH"
